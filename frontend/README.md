# Frontend 예약 영역

관리자 UI는 프로젝트 06에서 요구사항과 framework를 확정한 뒤 생성합니다. 현재는 application과 Node dependency를 두지 않습니다.

미래 UI는 versioned OpenAPI를 통해서만 backend를 호출하며 database나 backend repository에 접근하지 않습니다. 오류 분기는 사용자 메시지가 아닌 안정적인 오류 `code`를 사용합니다.
